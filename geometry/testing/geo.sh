# Just run this in wsl or gitbash terminal
g++ geo1.cpp -O2 -std=c++17 -o angle   # you can also use the compiler flags
./angle                                # or : ./angle < tests.txt > ans.txt  ... when freopen is not used